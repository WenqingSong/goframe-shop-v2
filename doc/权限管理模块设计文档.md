# 后台管理系统权限管理模块设计文档

## 一、概述

本项目采用 **RBAC（Role-Based Access Control，基于角色的访问控制）** 模型实现权限管理。核心思想是：
- **管理员（Admin）** 拥有一个或多个 **角色（Role）**
- **角色（Role）** 拥有一个或多个 **权限（Permission）**
- **权限（Permission）** 对应具体的 API 路径

通过这种设计，实现了用户与权限的解耦，便于灵活管理。

---

## 二、数据库表结构设计

### 2.1 管理员表 `admin_info`

**文件位置**: `hack/shop.sql` (第62-74行)

```sql
CREATE TABLE `admin_info` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `name` varchar(30) NOT NULL DEFAULT '' COMMENT '用户名',
  `password` varchar(50) NOT NULL DEFAULT '' COMMENT '密码',
  `role_ids` varchar(50) NOT NULL DEFAULT '' COMMENT '角色ids',
  `created_at` datetime NULL DEFAULT NULL,
  `updated_at` datetime NULL DEFAULT NULL,
  `user_salt` varchar(10) NOT NULL DEFAULT '' COMMENT '加密盐',
  `is_admin` tinyint(1) NOT NULL DEFAULT 0 COMMENT '是否超级管理员',
  PRIMARY KEY (`id`),
  UNIQUE INDEX `name_unique`(`name`) COMMENT '名字唯一索引'
);
```

**字段说明**:
| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 主键，自增 |
| `name` | varchar(30) | 用户名，唯一 |
| `password` | varchar(50) | 加密后的密码 |
| `role_ids` | varchar(50) | 角色ID列表，逗号分隔（如 "1,2,3"） |
| `user_salt` | varchar(10) | 密码加密盐 |
| `is_admin` | tinyint(1) | 是否超级管理员（1=是，0=否） |

**Entity 定义**: `internal/model/entity/admin_info.go`

```go
type AdminInfo struct {
    Id        int         `json:"id"`
    Name      string      `json:"name"      description:"用户名"`
    Password  string      `json:"password"  description:"密码"`
    RoleIds   string      `json:"roleIds"   description:"角色ids"`
    CreatedAt *gtime.Time `json:"createdAt"`
    UpdatedAt *gtime.Time `json:"updatedAt"`
    UserSalt  string      `json:"userSalt"  description:"加密盐"`
    IsAdmin   int         `json:"isAdmin"   description:"是否超级管理员"`
}
```

---

### 2.2 角色表 `role_info`

**文件位置**: `hack/shop.sql` (第552-562行)

```sql
CREATE TABLE `role_info` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `name` varchar(50) NOT NULL DEFAULT '' COMMENT '角色名称',
  `desc` varchar(255) NOT NULL COMMENT '描述',
  `created_at` datetime NULL DEFAULT NULL,
  `updated_at` datetime NULL DEFAULT NULL,
  `deleted_at` datetime NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE INDEX `unique_index`(`name`) COMMENT '角色昵称唯一索引'
);
```

**字段说明**:
| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 主键，自增 |
| `name` | varchar(50) | 角色名称，唯一 |
| `desc` | varchar(255) | 角色描述 |
| `deleted_at` | datetime | 软删除时间 |

**Entity 定义**: `internal/model/entity/role_info.go`

```go
type RoleInfo struct {
    Id        int         `json:"id"`
    Name      string      `json:"name"      description:"角色名称"`
    Desc      string      `json:"desc"      description:"描述"`
    CreatedAt *gtime.Time `json:"createdAt"`
    UpdatedAt *gtime.Time `json:"updatedAt"`
    DeletedAt *gtime.Time `json:"deletedAt"`
}
```

---

### 2.3 权限表 `permission_info`

**文件位置**: `hack/shop.sql` (第465-475行)

```sql
CREATE TABLE `permission_info` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `name` varchar(30) NOT NULL DEFAULT '' COMMENT '权限名称',
  `path` varchar(100) NOT NULL DEFAULT '' COMMENT '路径',
  `created_at` datetime NULL DEFAULT NULL,
  `updated_at` datetime NULL DEFAULT NULL,
  `deleted_at` datetime NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE INDEX `unique_name`(`name`) COMMENT '名称唯一索引'
);
```

**字段说明**:
| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 主键，自增 |
| `name` | varchar(30) | 权限名称，唯一 |
| `path` | varchar(100) | API 路径前缀（用于路径匹配） |
| `deleted_at` | datetime | 软删除时间 |

**Entity 定义**: `internal/model/entity/permission_info.go`

```go
type PermissionInfo struct {
    Id        int         `json:"id"`
    Name      string      `json:"name"      description:"权限名称"`
    Path      string      `json:"path"      description:"路径"`
    CreatedAt *gtime.Time `json:"createdAt"`
    UpdatedAt *gtime.Time `json:"updatedAt"`
    DeletedAt *gtime.Time `json:"deletedAt"`
}
```

---

### 2.4 角色-权限关联表 `role_permission_info`

**文件位置**: `hack/shop.sql` (第573-582行)

```sql
CREATE TABLE `role_permission_info` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `role_id` int(11) NOT NULL DEFAULT 0 COMMENT '角色id',
  `permission_id` int(11) NOT NULL COMMENT '权限id',
  `created_at` datetime NULL DEFAULT NULL,
  `updated_at` datetime NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE INDEX `unique_index`(`role_id`, `permission_id`) COMMENT '唯一索引'
);
```

**字段说明**:
| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 主键，自增 |
| `role_id` | int | 角色ID，外键关联 role_info.id |
| `permission_id` | int | 权限ID，外键关联 permission_info.id |

**Entity 定义**: `internal/model/entity/role_permission_info.go`

```go
type RolePermissionInfo struct {
    Id           int         `json:"id"`
    RoleId       int         `json:"roleId"       description:"角色id"`
    PermissionId int         `json:"permissionId" description:"权限id"`
    CreatedAt    *gtime.Time `json:"createdAt"`
    UpdatedAt    *gtime.Time `json:"updatedAt"`
}
```

---

### 2.5 表关系 ER 图

```
┌─────────────┐       ┌─────────────────────┐       ┌─────────────────┐
│ admin_info  │       │ role_permission_info│       │ permission_info │
├─────────────┤       ├─────────────────────┤       ├─────────────────┤
│ id          │       │ id                  │       │ id              │
│ name        │       │ role_id        ─────┼───┐   │ name            │
│ password    │       │ permission_id  ─────┼───┼───│ path            │
│ role_ids ───┼───┐   │ created_at          │   │   │ created_at      │
│ is_admin    │   │   │ updated_at          │   │   │ updated_at      │
│ user_salt   │   │   └─────────────────────┘   │   │ deleted_at      │
│ created_at  │   │                             │   └─────────────────┘
│ updated_at  │   │   ┌─────────────┐           │
└─────────────┘   │   │ role_info   │           │
                  │   ├─────────────┤           │
                  └──►│ id     ◄────┼───────────┘
                      │ name        │
                      │ desc        │
                      │ created_at  │
                      │ updated_at  │
                      │ deleted_at  │
                      └─────────────┘
```

**关系说明**:
- `admin_info.role_ids` → `role_info.id`（一对多，逗号分隔存储）
- `role_info.id` ↔ `permission_info.id`（多对多，通过 `role_permission_info` 关联）

---

## 三、核心功能实现

### 3.1 登录认证与 JWT Token 生成

**文件位置**: `internal/service/auth.go` (第18-90行)

登录时，系统会验证用户名密码，并将 `is_admin` 和 `role_ids` 存入 JWT Token 中：

```go
// Authenticator 登录验证
func Authenticator(ctx context.Context) (interface{}, error) {
    var (
        r  = g.RequestFromCtx(ctx)
        in model.UserLoginInput
    )
    if err := r.Parse(&in); err != nil {
        return "", err
    }
    // 调用 GetAdminByNamePasswordWithRoles 获取包含角色信息的用户数据
    if user := Admin().GetAdminByNamePasswordWithRoles(ctx, in); user != nil {
        return user, nil
    }
    return nil, jwt.ErrFailedAuthentication
}
```

**文件位置**: `internal/logic/admin/admin.go` (第171-187行)

```go
// GetAdminByNamePasswordWithRoles 登录验证并返回包含角色信息的数据（用于JWT存储）
func (s *sAdmin) GetAdminByNamePasswordWithRoles(ctx context.Context, in model.UserLoginInput) map[string]interface{} {
    adminInfo := entity.AdminInfo{}
    err := dao.AdminInfo.Ctx(ctx).Where("name", in.Name).Scan(&adminInfo)
    if err != nil {
        return nil
    }
    if utility.EncryptPassword(in.Password, adminInfo.UserSalt) != adminInfo.Password {
        return nil
    }
    return g.Map{
        "id":       adminInfo.Id,
        "username": adminInfo.Name,
        "is_admin": adminInfo.IsAdmin,  // 是否超级管理员
        "role_ids": adminInfo.RoleIds,  // 角色ID列表
    }
}
```

---

### 3.2 权限校验中间件

**文件位置**: `internal/logic/middleware/middleware.go` (第163-242行)

这是权限管理的核心逻辑，每个需要鉴权的请求都会经过此中间件：

```go
// 超管专属路径（权限管理模块）
var adminOnlyPaths = []string{
    "/backend/role",
    "/backend/permission",
    "/backend/admin",
    "/backend/user",
}

// 不需要权限校验的路径
var noPermissionCheckPaths = []string{
    "/backend/login",
    "/backend/logout",
    "/backend/admin/create",
    "/backend/refresh-token",
}

// PermissionCheck 权限校验中间件
func (s *sMiddleware) PermissionCheck(r *ghttp.Request) {
    ctx := r.Context()
    requestPath := r.URL.Path

    // 1. 检查是否是不需要权限校验的路径
    for _, path := range noPermissionCheckPaths {
        if strings.HasPrefix(requestPath, path) {
            r.Middleware.Next()
            return
        }
    }

    // 2. 从JWT中获取管理员信息
    claims := jwt.ExtractClaims(ctx)
    if claims == nil {
        response.JsonExit(r, 401, "未登录或登录已过期")
        return
    }

    isAdmin := gconv.Int(claims["is_admin"])
    roleIdsStr := gconv.String(claims["role_ids"])

    // 3. 超级管理员拥有所有权限
    if isAdmin == 1 {
        r.Middleware.Next()
        return
    }

    // 4. 检查是否是超管专属路径
    for _, adminPath := range adminOnlyPaths {
        if strings.HasPrefix(requestPath, adminPath) {
            response.JsonExit(r, 403, "权限不足，该功能仅超级管理员可访问")
            return
        }
    }

    // 5. 解析 role_ids（格式如 "1,2,3"）
    var roleIds []int
    if roleIdsStr != "" {
        roleIdStrs := strings.Split(roleIdsStr, ",")
        for _, idStr := range roleIdStrs {
            idStr = strings.TrimSpace(idStr)
            if idStr != "" {
                roleIds = append(roleIds, gconv.Int(idStr))
            }
        }
    }

    // 6. 如果没有角色，则无权限
    if len(roleIds) == 0 {
        response.JsonExit(r, 403, "权限不足，未分配角色")
        return
    }

    // 7. 获取该角色拥有的所有权限路径
    allowedPaths, err := service.Permission().GetPathsByRoleIds(ctx, roleIds)
    if err != nil {
        response.JsonExit(r, 500, "权限校验失败")
        return
    }

    // 8. 检查当前请求路径是否匹配任一权限路径（前缀匹配）
    for _, allowedPath := range allowedPaths {
        if strings.HasPrefix(requestPath, allowedPath) {
            r.Middleware.Next()
            return
        }
    }

    // 9. 没有匹配的权限
    response.JsonExit(r, 403, "权限不足，无法访问该功能")
}
```

---

### 3.3 根据角色ID获取权限路径

**文件位置**: `internal/logic/permission/permission.go` (第85-137行)

```go
// GetPathsByRoleIds 根据角色ID列表获取所有权限路径
func (s *sPermission) GetPathsByRoleIds(ctx context.Context, roleIds []int) ([]string, error) {
    if len(roleIds) == 0 {
        return []string{}, nil
    }

    // 1. 根据 role_ids 查询 role_permission_info 表获取 permission_ids
    var rolePermissions []entity.RolePermissionInfo
    err := dao.RolePermissionInfo.Ctx(ctx).
        WhereIn(dao.RolePermissionInfo.Columns().RoleId, roleIds).
        Scan(&rolePermissions)
    if err != nil {
        return nil, err
    }

    if len(rolePermissions) == 0 {
        return []string{}, nil
    }

    // 2. 提取 permission_ids 并去重
    permissionIdMap := make(map[int]bool)
    for _, rp := range rolePermissions {
        permissionIdMap[rp.PermissionId] = true
    }
    permissionIds := make([]int, 0, len(permissionIdMap))
    for id := range permissionIdMap {
        permissionIds = append(permissionIds, id)
    }

    // 3. 根据 permission_ids 查询 permission_info 表获取 path
    var permissions []entity.PermissionInfo
    err = dao.PermissionInfo.Ctx(ctx).
        WhereIn(dao.PermissionInfo.Columns().Id, permissionIds).
        Scan(&permissions)
    if err != nil {
        return nil, err
    }

    // 4. 提取所有 path
    paths := make([]string, 0, len(permissions))
    for _, p := range permissions {
        if p.Path != "" {
            paths = append(paths, p.Path)
        }
    }

    return paths, nil
}
```

---

## 四、API 接口设计

### 4.1 管理员管理

**API 定义文件**: `api/backend/admin.go`

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 创建管理员 | POST | `/backend/admin/add` | 创建新管理员 |
| 删除管理员 | DELETE | `/backend/admin/delete` | 删除管理员 |
| 修改管理员 | POST | `/backend/admin/update` | 修改管理员信息 |
| 管理员列表 | GET | `/backend/admin/list` | 获取管理员列表 |
| 当前管理员信息 | GET | `/backend/admin/info` | 获取当前登录管理员信息 |

**Controller 实现**: `internal/controller/admin.go`

**Logic 实现**: `internal/logic/admin/admin.go`

---

### 4.2 角色管理

**API 定义文件**: `api/backend/role.go`

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 创建角色 | POST | `/backend/role/add` | 创建新角色 |
| 删除角色 | DELETE | `/backend/role/delete` | 删除角色 |
| 修改角色 | POST | `/backend/role/update` | 修改角色信息 |
| 角色列表 | GET | `/backend/role/list` | 获取角色列表（含权限） |
| 添加权限 | POST | `/backend/role/add/permission` | 为角色添加单个权限 |
| 批量添加权限 | POST | `/backend/role/add/permissions` | 为角色批量添加权限 |
| 删除权限 | DELETE | `/backend/role/delete/permission` | 删除角色的单个权限 |
| 批量删除权限 | DELETE | `/backend/role/delete/permissions` | 批量删除角色的权限 |

**Controller 实现**: `internal/controller/role.go`

**Logic 实现**: `internal/logic/role/role.go`

---

### 4.3 权限管理

**API 定义文件**: `api/backend/permission.go`

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 创建权限 | POST | `/backend/permission/add` | 创建新权限 |
| 删除权限 | DELETE | `/backend/permission/delete` | 删除权限 |
| 修改权限 | POST | `/backend/permission/update` | 修改权限信息 |
| 权限列表 | GET | `/backend/permission/list` | 获取权限列表 |

**Controller 实现**: `internal/controller/permission.go`

**Logic 实现**: `internal/logic/permission/permission.go`

---

## 五、路由配置

**文件位置**: `internal/cmd/cmd.go` (第29-78行)

```go
// 管理后台路由组
s.Group("/backend", func(group *ghttp.RouterGroup) {
    group.Middleware(
        service.Middleware().CORS,
        service.Middleware().Ctx,
        service.Middleware().ResponseHandler,
    )
    // 不需要登录的路由组绑定
    group.Bind(
        controller.Admin.Create,       // 管理员创建
        controller.Login.Login,        // 登录
        controller.Login.RefreshToken, // 刷新Token
    )
    // 需要登录的路由组绑定（JWT 中间件 + 权限校验）
    group.Group("/", func(group *ghttp.RouterGroup) {
        group.Middleware(
            service.Middleware().Auth,            // JWT 认证
            service.Middleware().PermissionCheck, // 权限校验
        )
        group.Bind(
            controller.Role,       // 角色管理
            controller.Permission, // 权限管理
            controller.Admin.List,
            controller.Admin.Update,
            controller.Admin.Delete,
            // ... 其他需要权限的接口
        )
    })
})
```

---

## 六、权限校验流程图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           请求进入                                       │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    是否为免登录路径？                                     │
│            (/backend/login, /backend/logout 等)                          │
└─────────────────────────────────────────────────────────────────────────┘
                          │                    │
                         是                   否
                          │                    │
                          ▼                    ▼
                    ┌──────────┐    ┌─────────────────────┐
                    │ 直接放行  │    │  JWT 认证中间件      │
                    └──────────┘    │  (Auth Middleware)   │
                                    └─────────────────────┘
                                               │
                                               ▼
                                    ┌─────────────────────┐
                                    │  从 JWT 提取 claims  │
                                    │  is_admin, role_ids │
                                    └─────────────────────┘
                                               │
                                               ▼
                                    ┌─────────────────────┐
                                    │  is_admin == 1 ?    │
                                    └─────────────────────┘
                                      │              │
                                     是             否
                                      │              │
                                      ▼              ▼
                                ┌──────────┐  ┌─────────────────────┐
                                │ 直接放行  │  │ 是否为超管专属路径？  │
                                └──────────┘  │ (/backend/role 等)   │
                                              └─────────────────────┘
                                                    │          │
                                                   是         否
                                                    │          │
                                                    ▼          ▼
                                              ┌──────────┐ ┌─────────────────┐
                                              │ 403 拒绝  │ │ 解析 role_ids   │
                                              └──────────┘ └─────────────────┘
                                                                  │
                                                                  ▼
                                                    ┌─────────────────────────┐
                                                    │ 查询 role_permission_info│
                                                    │ 获取 permission_ids      │
                                                    └─────────────────────────┘
                                                                  │
                                                                  ▼
                                                    ┌─────────────────────────┐
                                                    │ 查询 permission_info     │
                                                    │ 获取所有 path            │
                                                    └─────────────────────────┘
                                                                  │
                                                                  ▼
                                                    ┌─────────────────────────┐
                                                    │ 请求路径是否匹配任一 path │
                                                    │ （前缀匹配）              │
                                                    └─────────────────────────┘
                                                          │          │
                                                         是         否
                                                          │          │
                                                          ▼          ▼
                                                    ┌──────────┐ ┌──────────┐
                                                    │   放行    │ │ 403 拒绝 │
                                                    └──────────┘ └──────────┘
```

---

## 七、代码文件索引

### 7.1 数据库相关

| 文件 | 说明 |
|------|------|
| `hack/shop.sql` | 数据库表结构定义 |
| `internal/model/entity/admin_info.go` | 管理员实体 |
| `internal/model/entity/role_info.go` | 角色实体 |
| `internal/model/entity/permission_info.go` | 权限实体 |
| `internal/model/entity/role_permission_info.go` | 角色-权限关联实体 |
| `internal/dao/admin_info.go` | 管理员 DAO |
| `internal/dao/role_info.go` | 角色 DAO |
| `internal/dao/permission_info.go` | 权限 DAO |
| `internal/dao/role_permission_info.go` | 角色-权限关联 DAO |

### 7.2 API 定义

| 文件 | 说明 |
|------|------|
| `api/backend/admin.go` | 管理员 API 定义 |
| `api/backend/role.go` | 角色 API 定义 |
| `api/backend/permission.go` | 权限 API 定义 |

### 7.3 Model 定义

| 文件 | 说明 |
|------|------|
| `internal/model/admin.go` | 管理员业务模型 |
| `internal/model/role.go` | 角色业务模型 |
| `internal/model/permission.go` | 权限业务模型 |

### 7.4 Controller 层

| 文件 | 说明 |
|------|------|
| `internal/controller/admin.go` | 管理员控制器 |
| `internal/controller/role.go` | 角色控制器 |
| `internal/controller/permission.go` | 权限控制器 |

### 7.5 Logic/Service 层

| 文件 | 说明 |
|------|------|
| `internal/logic/admin/admin.go` | 管理员业务逻辑 |
| `internal/logic/role/role.go` | 角色业务逻辑 |
| `internal/logic/permission/permission.go` | 权限业务逻辑 |
| `internal/logic/middleware/middleware.go` | 中间件（含权限校验） |
| `internal/service/auth.go` | JWT 认证服务 |

### 7.6 路由配置

| 文件 | 说明 |
|------|------|
| `internal/cmd/cmd.go` | 路由注册与中间件配置 |

---

## 八、使用示例

### 8.1 创建权限

```bash
POST /backend/permission/add
{
    "name": "商品管理",
    "path": "/backend/goods"
}
```

### 8.2 创建角色并分配权限

```bash
# 创建角色
POST /backend/role/add
{
    "name": "商品管理员",
    "desc": "负责商品相关管理"
}

# 批量添加权限
POST /backend/role/add/permissions
{
    "role_id": 2,
    "permission_ids": [1, 2, 3]
}
```

### 8.3 创建管理员并分配角色

```bash
POST /backend/admin/add
{
    "name": "zhangsan",
    "password": "123456",
    "role_ids": "2,3",
    "is_admin": 0
}
```

---

## 九、注意事项

1. **超级管理员**：`is_admin = 1` 的管理员拥有所有权限，不受权限校验限制
2. **路径匹配**：权限校验采用**前缀匹配**，如权限路径为 `/backend/goods`，则 `/backend/goods/list`、`/backend/goods/add` 等都会匹配
3. **角色ID存储**：`admin_info.role_ids` 使用逗号分隔的字符串存储，如 `"1,2,3"`
4. **超管专属路径**：`/backend/role`、`/backend/permission`、`/backend/admin`、`/backend/user` 仅超级管理员可访问
5. **JWT Token**：登录时会将 `is_admin` 和 `role_ids` 存入 JWT，权限校验时从 JWT 中提取
